<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Required on Android 13+ to show the generic "new message" notification
         a push wake triggers (see lib/push/push_manager.dart). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- QR scanning (mobile_scanner), used by the setup wizard's scan
         entry point, see lib/screens/qr_scan_screen.dart. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <application
        android:label="Freizone"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- launchMode singleTask (NOT singleInstance): both make a second
             launch return to the existing instance instead of duplicating
             the task, but singleInstance forbids any other activity in the
             same task, which silently discards startActivityForResult
             results: the photo picker (image_picker) always returned
             null. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Receiving a share from another app (APP-15). Freizone appears in
                 the system share sheet for text and images; the target account
                 and conversation are chosen in-app afterwards, since a plain
                 ACTION_SEND carries no target. launchMode="singleTask" above is
                 what makes a share into an already-running app arrive at
                 onNewIntent rather than a second activity instance. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
            <!-- Points at the <share-target> declaration that turns our
                 per-chat dynamic shortcuts into direct-share targets
                 (APP-15 level 2, res/xml/shortcuts.xml). -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity>

        <!-- APP-12: handle a refreshed FCM token even with the app closed.
             Exactly one service may claim com.google.firebase.MESSAGING_EVENT,
             so firebase_messaging's own declaration is removed in favour of
             our subclass of it (which still calls super, keeping the
             foreground onTokenRefresh stream working). Two services claiming
             the same event would be undefined behaviour, the kind of bug
             that looks like "push works on my device". Message delivery is
             unaffected: the plugin handles that in its own broadcast
             receiver, which stays. -->
        <service
            android:name="io.flutter.plugins.firebase.messaging.FlutterFirebaseMessagingService"
            tools:node="remove"/>
        <service
            android:name=".FreizonePushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FCM's own default status-bar icon for background notifications
             it delivers natively (before any Dart code runs). Without
             this, FCM falls back to the application's @mipmap/ic_launcher,
             showing that icon's silhouette (a plain filled circle) instead
             of a recognizable shape. See lib/push/push_manager.dart's
             AndroidInitializationSettings for the equivalent fix on the
             flutter_local_notifications side. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher (APP-14): from Android 11 on, other apps
             are invisible unless queried for, and canLaunchUrl/launchUrl then
             fail silently rather than opening the browser or mail client. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
